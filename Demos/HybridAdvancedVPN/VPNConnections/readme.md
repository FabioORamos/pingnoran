The two config files were downloaded from the AWS Console - VPC | Site-to-Site VPN Connections. 

These configurations files were then used to configure the two EC2 instances running the ONPREM-ROUTER.